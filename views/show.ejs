<ol>
    <% astras.forEach(function(astra) { %>
        <li>
            <h3>
                <a href="/data/<%=astra.name%>"><%= astra.name %></a>
            </h3>
        </li>
    <% }); %>
</ol>
